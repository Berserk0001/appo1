"use strict";const MIN_COMPRESS_LENGTH=1024;const MIN_TRANSPARENT_COMPRESS_LENGTH=MIN_COMPRESS_LENGTH*100;export function shouldCompress(e){const{originType:r,originSize:s,webp:t}=e.params;if(!r.startsWith("image"))return false;if(s===0)return false;if(e.headers.range)return false;if(t&&s<MIN_COMPRESS_LENGTH)return false;if(!t&&(r.endsWith("png")||r.endsWith("gif"))&&s<MIN_TRANSPARENT_COMPRESS_LENGTH){return false}return true}
